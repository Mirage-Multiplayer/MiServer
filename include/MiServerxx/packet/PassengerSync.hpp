#pragma once
#include <MiRak/NetworkTypes.h>

namespace mimp
{
	namespace internal
	{
		namespace packet
		{
			/**
			 * @brief Passenger Sync Packet
			 */
			void PassengerSync(Packet *pkt);
		}
	}
}